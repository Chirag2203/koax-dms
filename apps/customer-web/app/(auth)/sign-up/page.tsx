import { SignUpForm } from '@/src/components/portal/auth/sign-up-form';

export const metadata = {
  title: 'Create Account — BN Automobiles',
};

export default function SignUpPage() {
  return <SignUpForm />;
}
