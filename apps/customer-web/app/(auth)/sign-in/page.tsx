import { SignInForm } from '@/src/components/portal/auth/sign-in-form';

export const metadata = {
  title: 'Sign In — BN Automobiles',
};

export default function SignInPage() {
  return <SignInForm />;
}
