import { CustomBuildsVendorsPage } from '@/src/components/custom-builds/vendors/custom-builds-vendors-page';

/**
 * /custom-builds/vendors — Vendor directory.
 *
 * Spec reference: SPEC-CUSTOM-BUILDS-001 §8
 */
export default function VendorsPage() {
  return <CustomBuildsVendorsPage />;
}
