'use client';

import { CustomersIndexView } from '@/src/components/customers/customers-index-view';

/**
 * /customers — full index page (replaces stub).
 * Spec reference: SPEC-CUSTOMERS-001 §6
 */
export default function CustomersPage() {
  return <CustomersIndexView />;
}
